{
  "name": "xampp",
  "version": "1.0.0",
  "description": "```bash sudo nano /etc/hosts ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^3.13.0"
  }
}
